# Marketing preview videos (`frontend/public/`)

Used on the public marketing site:

| File | Where |
|------|--------|
| `case-workflow-preview.mp4` | Landing hero + Features “See it in action” |
| `semantic-search-preview.mp4` | Features “See it in action” |

Paths are resolved with `import.meta.env.BASE_URL` via `publicAsset()` in the app.
